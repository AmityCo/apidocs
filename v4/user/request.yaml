assign-tags-to-user-request-payload:
  description: Information for assigning tags to a user
  required: true
  content:
    application/json:
      schema:
        $ref: '../../schema/tag.yaml#/AssignTagsToUserRequest'

assign-tag-to-users-request-payload:
  description: Information for assigning a tag to multiple users
  required: true
  content:
    application/json:
      schema:
        $ref: '../../schema/tag.yaml#/AssignTagToUsersRequest'
