UserDeleteResponse:
  description: return success:'true' when deletion is successful
  content:
    application/json:
      schema:
        type: object
        properties:
          success:
            type: boolean
            example: true