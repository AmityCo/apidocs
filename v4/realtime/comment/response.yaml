Response:
  description: Comment Information
  content:
    application/json:
      schema:
        $ref: './schema.yaml#/Comment'

ResponseWithFlag:
  description: Comment Information
  content:
    application/json:
      schema:
        $ref: './schema.yaml#/CommentWithFlag'

ResponseWithReactor:
  description: Comment Information
  content:
    application/json:
      schema:
        $ref: './schema.yaml#/CommentWithReactor'
