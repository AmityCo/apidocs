Response:
  description: Story Information
  content:
    application/json:
      schema:
        $ref: './schema.yaml#/Story'

ResponseWithFlag:
  description: Story Information
  content:
    application/json:
      schema:
        $ref: './schema.yaml#/StoryWithFlag'

ResponseWithReactor:
  description: Story Information
  content:
    application/json:
      schema:
        $ref: './schema.yaml#/StoryWithReactor'
