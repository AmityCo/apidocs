blacklist-records-request-payload:
  required: true
  content:
    application/json:
      schema:
        type: "object"
        properties:
          regexs:
            type: "array"
            items:
              type: "string"
        required:
          - "regexs"
