blacklist-records-request-payload:
  required: true
  content:
    application/json:
      schema:
        type: "object"
        properties:
          regexs:
            type: "array"
            maxItems: 10000
            items:
              type: "string"
        required:
          - "regexs"
