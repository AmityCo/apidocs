QueryAdminResponsePayload:
  description: Query admin response payload
  type: object
  properties:
    users:
      type: array
      items:
        $ref: "../../schema/user.yaml#/AdminUser"
    files:
      type: array
      items:
        $ref: "../../schema/file.yaml#/File"
    paging:
      type: object
      properties:
        next:
          type: string
        previous:
          type: string
