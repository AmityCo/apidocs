GetConfidenceSettingResponse:
  description: Get post-moderation settings response
  content:
    application/json:
      schema:
        type: object
        properties:
          confidences:
            type: array
            items:
              $ref: '../../schema/moderation.yaml#/Confidence'
        required:
          - confidences
