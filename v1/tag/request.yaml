create-tag-request-payload:
  description: Information for creating a new tag
  required: true
  content:
    application/json:
      schema:
        $ref: '../../schema/tag.yaml#/CreateTagRequest'

update-tag-request-payload:
  description: Information for updating an existing tag
  required: true
  content:
    application/json:
      schema:
        $ref: '../../schema/tag.yaml#/UpdateTagRequest'
