premium-reports-get-response-200:
  description: |
    Premium report settings response.
  content:
    application/json:
      schema:
        $ref: "../../schema/network-setting.yaml#/PremiumReportNetworkSetting"
