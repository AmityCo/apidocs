SessionResponse:

GetSessionResponse:
  description: Session Information
  content:
    application/json:
      schema:
        type: object
        properties:
          lastCachePurge:
            type: string
            description: Date of the last cache purge