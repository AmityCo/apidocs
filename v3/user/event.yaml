didUpdate:
  tags:
    - User
  security:
        - SocketAuth: []
  description: >
    Publish this event following endpoint below:
      * `update user`
      * `update user(v3)`
  responses:
    "200000":
      $ref: "./response.yaml#/UserSocketResponse"