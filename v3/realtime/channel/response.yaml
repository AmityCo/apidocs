Response:
  description: Channel Information
  content:
    application/json:
      schema:
        $ref: './schema.yaml#/Channel'

RoleChangedResponse:
  description: The affected members with the roles and permissions they now hold
  content:
    application/json:
      schema:
        allOf:
          - type: object
            properties:
              id:
                type: string
                description: The message ID.
              data:
                type: object
                properties:
                  channelId:
                    type: string
                  setterId:
                    type: string
                    description: ID of the user that changed the roles.
                  roles:
                    type: array
                    description: The roles named in the request that caused the event.
                    items:
                      type: string
                  users:
                    type: array
                    description: The affected members, each with the roles they hold after the change.
                    items:
                      type: object
                      properties:
                        userId:
                          type: string
                        roles:
                          type: array
                          items:
                            type: string
                        permissions:
                          type: array
                          items:
                            type: string
                  channels:
                    type: array
                    items:
                      $ref: "../../../schema/channel.yaml#/ChannelV3"
          - $ref: '../../../schema/realtime.yaml#/Wrapper'
