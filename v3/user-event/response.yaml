VideoStreamingUserEventApiResponse:
  description: event is processed successfully
  content:
    application/json:
      schema:
        properties:
          ok:
            type: boolean
            example: true

