video-streaming.didRecord:
  get:
    tags:
      - "Webhook event"
    security:
      - BearerAuth: []
    description: "This is called when video streaming recordings is ready.\n"
    responses:
      "200":
        $ref: "../../v3/video-streaming/response.yaml#/video-streaming-webhook-response"
video-streaming.didStart:
  get:
    tags:
      - "Webhook event"
    security:
      - BearerAuth: []
    description: "This is called when video streaming start.\n"
    responses:
      "200":
        $ref: "../../v3/video-streaming/response.yaml#/video-streaming-webhook-response"
video-streaming.didStop:
  get:
    tags:
      - "Webhook event"
    security:
      - BearerAuth: []
    description: "This is called when video streaming stop.\n"
    responses:
      "200":
        $ref: "../../v3/video-streaming/response.yaml#/video-streaming-webhook-response"
