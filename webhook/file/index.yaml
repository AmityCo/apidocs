file.didDelete:
  get:
    tags:
      - "Webhook event"
    summary: "File deleted webhook event"
    security:
      - BearerAuth: []
    description: "This is called when file is deleted\n"
    responses:
      200:
        $ref: "../../v3/file/response.yaml#/_fileId-get-response-200"
