Content:
  type: object
  properties:
    contentId:
      type: string
      description: ID of the content.
    contentType:
      type: string
      enum:
        - post
        - comment
        - message
      description: Type of the content.
