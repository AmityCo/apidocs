Content:
  type: object
  properties:
    contentId:
      type: string
      description: Public ID of the content.
    contentType:
      type: string
      enum:
        - post
        - comment
      description: Type of the content.
