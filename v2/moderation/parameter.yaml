authorization:
  name: "Authorization"
  in: "header"
  required: true
  schema:
    type: "string"

content-type:
  name: "Content-Type"
  in: "header"
  required: true
  schema:
    type: "string"

regexs:
  name: "regexs"
  in: "query"
  schema:
    type: "array"
    items:
      type: "string"

ruleIds:
  in: query
  name: ruleIds
  description: "Rules information to be deleted.\n"
  schema:
    type: "array"
    items:
      type: "string"

ruleId:
  in: path
  name: ruleId
  description: "Rule ID to be updated."
  required: true
  schema:
    type: string
