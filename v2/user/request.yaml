common-post-request-payload:
  required: true
  content:
    application/json:
      schema:
        type: "object"
        properties:
          userId:
            type: "string"
        required:
          - "userId"
